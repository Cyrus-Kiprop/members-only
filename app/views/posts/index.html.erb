<%# <p id="notice"><%= notice %></p>

<h1>Posts</h1>

<section>
  <div class="container">
    <div class="row">
      <div class="col-3">
        this section if the followers
      </div>

      <div class="col-6 p-0">
        <%= render 'member_feed'  %>
      </div>

      <div class="col-3">
        this third of the followers
      </div>
    </div>
  </div>
</section>

<!-- <table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= link_to 'Show', post %></td>
        <td><%= link_to 'Edit', edit_post_path(post) %></td>
        <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table> -->

<br>

<%= link_to 'New Post', new_post_path %>
